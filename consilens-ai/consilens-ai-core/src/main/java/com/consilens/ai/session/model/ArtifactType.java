package com.consilens.ai.session.model;

/**
 * Artifact types emitted by the AI runtime.
 */
public enum ArtifactType {
    CONFIG,
    VALIDATION,
    DRY_RUN,
    DIFF_RESULT,
    DIFF_EVIDENCE,
    DIAGNOSIS,
    RUN_AUDIT,
    REPAIR_PATCH,
    APPROVAL
}
