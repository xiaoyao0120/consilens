package com.consilens.ai.conversation.engine.model;

/**
 * High-level planner output category before runtime action mapping.
 */
public enum PlannerType {
    PLAN,
    CHAT,
    QUESTION,
    ERROR
}
