# MySQL 到 TiDB 跨数据库 Checksum 比对配置
# TiDB 使用 MySQL 协议，高度兼容
#
# 前置条件：
#   1. MySQL 中已加载 examples/seed-sql/load-mysql-consilens-demo-data.sql
#   2. TiDB 中已加载 examples/seed-sql/load-tidb-consilens-demo-data.sql

source:
  type: mysql
  name: source-mysql
  connection:
    url: jdbc:mysql://localhost:13306/consilens_demo?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${env.MYSQL_USER}
    password: ${env.MYSQL_PASSWORD}
  resource:
    type: table
    name: consilens_performance_demo_table

target:
  type: tidb
  name: target-tidb
  connection:
    url: jdbc:mysql://localhost:4000/consilens_demo?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${env.TIDB_USER}
    password: ${env.TIDB_PASSWORD}
  resource:
    type: table
    name: consilens_performance_demo_table

comparison:
  keys:
    source:
      - record_id
    target:
      - record_id
  fields:
    source:
      - col_tinyint
      - col_smallint
      - col_mediumint
      - col_int
      - col_bigint
      - col_decimal
      - col_numeric
      - col_varchar_50
      - col_varchar_100
      - col_varchar_255
      - col_date
      - col_datetime
      - col_timestamp
      - user_name
      - email
      - phone
      - amount
      - balance
      - credit_limit
      - status
      - category
      - priority
      - score
      - created_at
      - updated_at
    target:
      - col_tinyint
      - col_smallint
      - col_mediumint
      - col_int
      - col_bigint
      - col_decimal
      - col_numeric
      - col_varchar_50
      - col_varchar_100
      - col_varchar_255
      - col_date
      - col_datetime
      - col_timestamp
      - user_name
      - email
      - phone
      - amount
      - balance
      - credit_limit
      - status
      - category
      - priority
      - score
      - created_at
      - updated_at

strategy:
  mode: checksum
  algorithm: xor
  bisectionFactor: 8
  bisectionThreshold: 8000
  batchSize: 1000
  enableProfiling: true

result:
  sinks:
    - format: console
      type: result
