# MySQL vs ClickHouse 自定义 SQL Checksum 比对配置
# 目标：验证 resource.type=sql 与 table 同级，并优先走 checksum。
#
# 前置条件：
#   1. MySQL 中已加载 load-mysql.sql
#   2. ClickHouse 中已加载 load-clickhouse.sql

source:
  type: mysql
  name: source-mysql-custom-sql
  connection:
    url: jdbc:mysql://127.0.0.1:13306/consilens_demo?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${env.MYSQL_USER}
    password: ${env.MYSQL_PASSWORD}
  resource:
    type: sql
    path: |
      SELECT record_id,
        col_tinyint,
        col_int,
        col_decimal,
        amount,
        status,
        updated_at
      FROM consilens_demo.consilens_performance_demo_table

target:
  type: clickhouse
  name: target-clickhouse-custom-sql
  connection:
    url: jdbc:clickhouse://127.0.0.1:8123/consilens_demo
    username: ${env.CLICKHOUSE_USER}
    password: ${env.CLICKHOUSE_PASSWORD}
  resource:
    type: sql
    path: |
      SELECT record_id,
        col_tinyint,
        col_int,
        col_decimal,
        amount,
        status,
        updated_at
      FROM consilens_demo.consilens_performance_demo_table

comparison:
  keys:
    source:
      - record_id
    target:
      - record_id

strategy:
  mode: checksum
  algorithm: xor
  bisectionFactor: 8
  bisectionThreshold: 8000
  batchSize: 1000
  enableProfiling: true

result:
  sinks:
    - format: console
      type: result
