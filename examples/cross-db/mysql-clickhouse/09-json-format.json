{
  "source": {
    "type": "mysql",
    "name": "source-mysql",
    "connection": {
      "url": "jdbc:mysql://127.0.0.1:13306/consilens_demo?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true",
      "username": "${env.MYSQL_USER}",
      "password": "${env.MYSQL_PASSWORD}"
    },
    "resource": {
      "type": "table",
      "name": "consilens_performance_demo_table"
    }
  },
  "target": {
    "type": "clickhouse",
    "name": "target-clickhouse",
    "connection": {
      "url": "jdbc:clickhouse://127.0.0.1:8123/consilens_demo",
      "username": "${env.CLICKHOUSE_USER}",
      "password": "${env.CLICKHOUSE_PASSWORD}"
    },
    "resource": {
      "type": "table",
      "name": "consilens_performance_demo_table"
    }
  },
  "comparison": {
    "keys": {
      "source": [
        "record_id"
      ],
      "target": [
        "record_id"
      ]
    },
    "fields": {
      "source": [
        "col_tinyint",
        "col_int",
        "col_decimal",
        "amount",
        "status",
        "updated_at"
      ],
      "target": [
        "col_tinyint",
        "col_int",
        "col_decimal",
        "amount",
        "status",
        "updated_at"
      ]
    }
  },
  "strategy": {
    "mode": "checksum",
    "algorithm": "xor",
    "bisectionFactor": 8,
    "bisectionThreshold": 8000,
    "batchSize": 1000,
    "enableProfiling": true
  },
  "result": {
    "sinks": [
      {
        "format": "console",
        "type": "result"
      }
    ]
  }
}
