# UNIT-SQL-GENERATION 实现者任务

负责 round-2 SQL 生成与规范化修复。必须阅读任务简报、实现计划、工作拆分、本单元 scope、最新 repair-request、round-1 review 和仓库指令。只能修改 scope 拥有或允许的文件；不得 stage、commit、push。必须补充定向测试并如实更新 resume-state、worklog、change-summary、validation。
