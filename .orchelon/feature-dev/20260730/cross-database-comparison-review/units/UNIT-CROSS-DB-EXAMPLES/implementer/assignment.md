# UNIT-CROSS-DB-EXAMPLES 实现者任务

负责 round-2 CLI、测试、脚本和 fixture 修复。必须阅读任务简报、实现计划、工作拆分、本单元 scope、最新 repair-request、round-1 review 和仓库指令。只能修改 scope 拥有或允许的文件；不得 stage、commit、push。不得修改已经正确的 Compose 挂载或两个 OceanBase 比较测试。必须如实更新 resume-state、worklog、change-summary、validation。
